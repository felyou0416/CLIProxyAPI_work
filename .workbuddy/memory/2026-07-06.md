## 2026-07-06

### 修复 Video 生成字段名不匹配
- **问题**: 前端发送 `num_frames` / `frame_rate` 在顶层，但 Agnes API 期望在 `extra_body` 内
- **修改**: `CLIProxyAPI-MediaProxy/main.go` `buildDirectPayload` 的 `agnes-video` 分支，将 `num_frames`, `frame_rate`, `size`, `max_tokens` 移入 `extra_body`
- **未修**: 超时 60s 问题（用户表示暂不修）
